{
  "name": "prisma-cte-bug",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "generate": "dotenv -- prisma generate --sql",
    "push": "dotenv -- prisma db push",
    "start-local-db": "docker compose up --wait"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.2.1",
    "dotenv-cli": "^8.0.0",
    "prisma": "^6.2.1"
  }
}
